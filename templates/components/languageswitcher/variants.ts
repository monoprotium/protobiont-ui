// the menu-presentation trigger reads like a quiet select control: a hairline
// surface with the current endonym + an optional globe (NEVER a flag).
export const languageTriggerClass =
  'inline-flex items-center gap-2 h-9 px-3 rounded-control border border-edge bg-surface-2 text-ink prt-motion-colors prt-ring hover:bg-surface-3'
