// Audio recorder look — plain class constants (no variant axes worth a CVA table).

export const audioRecorderClass =
  'flex flex-col gap-3 rounded-surface border border-edge bg-surface-1 p-4'

// compact = a slim recorder for embedding in a composer/toolbar (the recorder
// usually lives inside a larger interface — so it earns a tight, low-chrome form)
export const audioRecorderCompactClass =
  'flex flex-col gap-2 rounded-surface border border-edge bg-surface-1 px-3 py-2'

export const recorderTimeClass = 'text-sm font-mono tabular-nums text-ink'
