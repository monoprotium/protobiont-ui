{
  "name": "protobiont-ui",
  "private": true,
  "type": "module",
  "workspaces": ["cli", "demo"],
  "scripts": {
    "test": "bun test",
    "typecheck": "tsgo --noEmit",
    "registry": "bun scripts/build-registry.ts",
    "verify:fresh": "bun scripts/verify-fresh.ts",
    "lint:rack": "bun scripts/lint-rack.ts",
    "lint:imports": "bun scripts/lint-imports.ts",
    "dev": "bun --cwd demo dev",
    "build:cli": "bun --cwd cli build"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@typescript/native-preview": "^7.0.0-dev.20260612.1",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "@vueuse/core": "^14.3.0"
  }
}
